"use client";

import { useState } from "react";
export default function ScenariosPage(){const [applied,setApplied]=useState(false);return <><header className="page-head"><div><h2>Scenario controls</h2><p>Local UI preview. Backend control endpoints are not connected yet.</p></div></header><section className="panel"><div className="panel-head"><h3>Mock simulation parameters</h3><span>Later MVP</span></div><div className="panel-body"><div className="notice" style={{marginBottom:18}}>Changes remain local until the backend implements the mock-control contract.</div><div className="form-grid"><div className="field"><label htmlFor="robot-count">Robot count</label><input id="robot-count" type="number" min="1" max="10" defaultValue="5"/></div><div className="field"><label htmlFor="task-interval">Task interval (seconds)</label><input id="task-interval" type="number" min="1" defaultValue="8"/></div><div className="field"><label htmlFor="robot-speed">Robot speed (m/s)</label><input id="robot-speed" type="number" step="0.1" defaultValue="1.2"/></div><div className="field"><label htmlFor="sim-speed">Simulation speed</label><select id="sim-speed" defaultValue="1"><option value="1">1×</option><option value="2">2×</option><option value="4">4×</option></select></div><div className="field"><label htmlFor="drain">Battery drain mode</label><select id="drain"><option>Normal</option><option>Accelerated</option><option>Disabled</option></select></div></div><div className="button-row"><button className="button">Reset</button><button className="button primary" onClick={()=>setApplied(true)}>Apply preview</button></div>{applied&&<div className="notice" style={{marginTop:14}}>Preview saved locally. No command was sent to the simulator.</div>}</div></section></>}
